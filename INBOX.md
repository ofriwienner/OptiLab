# Feature Inbox

Add feature requests below as bullet points. Claude reads this during bulk features runs, converts each item to a GitHub Issue, and clears the list.

---

- when moving a group, the current behavior is 'jumpy' because the components try to stay on the grid. i want a group to move together as a rigid object.